---
phase: 01-foundation-and-agent-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/api/package.json
  - packages/api/src/auth.ts
  - packages/api/src/routes/auth.ts
  - packages/api/src/middleware/auth.ts
  - packages/api/src/index.ts
  - packages/api/src/__tests__/auth-email.test.ts
  - packages/api/src/__tests__/auth-session.test.ts
  - packages/api/src/__tests__/auth-github.test.ts
  - packages/api/src/__tests__/api-keys.test.ts
  - packages/api/src/__tests__/setup.ts
  - apps/web/package.json
  - apps/web/src/lib/auth-client.ts
  - apps/web/src/app/(auth)/sign-in/page.tsx
  - apps/web/src/app/(auth)/sign-up/page.tsx
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/app/dashboard/page.tsx
  - apps/web/src/app/settings/page.tsx
  - apps/web/src/components/auth-form.tsx
  - apps/web/src/components/api-key-manager.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "User can sign up with email and password via the web UI"
    - "User can sign in with email and password and session persists across browser refresh"
    - "User can sign in via GitHub OAuth"
    - "User can generate, view, and revoke API keys from account settings"
    - "API endpoints are protected by session or API key authentication"
  artifacts:
    - path: "packages/api/src/auth.ts"
      provides: "Better Auth server instance with email/password, GitHub OAuth, API key plugins"
      exports: ["auth"]
    - path: "packages/api/src/routes/auth.ts"
      provides: "Hono routes mounting Better Auth handler"
      exports: ["authRoutes"]
    - path: "packages/api/src/middleware/auth.ts"
      provides: "Session and API key verification middleware for Hono"
      exports: ["requireAuth"]
    - path: "apps/web/src/lib/auth-client.ts"
      provides: "Better Auth client for Next.js frontend"
      exports: ["authClient"]
    - path: "apps/web/src/app/settings/page.tsx"
      provides: "API key management UI"
      min_lines: 40
  key_links:
    - from: "packages/api/src/auth.ts"
      to: "@repo/db"
      via: "drizzleAdapter(db)"
      pattern: "drizzleAdapter"
    - from: "packages/api/src/routes/auth.ts"
      to: "packages/api/src/auth.ts"
      via: "auth.handler"
      pattern: "auth\\.handler"
    - from: "apps/web/src/lib/auth-client.ts"
      to: "packages/api/src/routes/auth.ts"
      via: "createAuthClient baseURL"
      pattern: "createAuthClient"
    - from: "packages/api/src/middleware/auth.ts"
      to: "packages/api/src/auth.ts"
      via: "auth.api.getSession"
      pattern: "getSession"

user_setup:
  - service: github-oauth
    why: "GitHub OAuth sign-in (AUTH-04)"
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub Settings -> Developer settings -> OAuth Apps -> New OAuth App"
      - name: GITHUB_CLIENT_SECRET
        source: "Same OAuth App page, generate a client secret"
    dashboard_config:
      - task: "Create OAuth App with callback URL http://localhost:3001/api/auth/callback/github"
        location: "GitHub Settings -> Developer settings -> OAuth Apps"
---

<objective>
Implement the complete authentication system: Better Auth server with email/password, GitHub OAuth, and API key plugins on the Hono API, plus the Next.js frontend with sign-up, sign-in, dashboard, and API key management pages.

Purpose: Authentication gates all user-facing features. Agent upload (Plan 03) requires authenticated endpoints.
Output: Working auth flow where users can register, sign in (email or GitHub), manage API keys, and access protected routes.
</objective>

<execution_context>
@C:/Users/basti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/basti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-agent-pipeline/01-RESEARCH.md
@.planning/phases/01-foundation-and-agent-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From packages/db/src/index.ts:
```typescript
export { db } from "./client"
export * from "./schema"
```

From packages/shared/src/types/api.ts:
```typescript
export interface ApiResponse<T> {
  success: boolean
  data?: T
  error?: string
}
```

From packages/api/src/index.ts:
```typescript
// Hono app with CORS and logger middleware, GET /health route
import { Hono } from "hono"
const app = new Hono()
export default app
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Better Auth server + Hono routes + auth middleware + tests</name>
  <files>
    packages/api/package.json,
    packages/api/src/auth.ts,
    packages/api/src/routes/auth.ts,
    packages/api/src/middleware/auth.ts,
    packages/api/src/index.ts,
    packages/api/src/__tests__/setup.ts,
    packages/api/src/__tests__/auth-email.test.ts,
    packages/api/src/__tests__/auth-session.test.ts,
    packages/api/src/__tests__/api-keys.test.ts
  </files>
  <behavior>
    - Test: POST /api/auth/sign-up with {email, password, name} returns 200 and creates user in DB
    - Test: POST /api/auth/sign-in/email with valid credentials returns 200 with session cookie
    - Test: POST /api/auth/sign-in/email with wrong password returns 401
    - Test: GET /api/auth/get-session with valid session cookie returns user data
    - Test: GET /api/auth/get-session without cookie returns 401/null
    - Test: POST /api/auth/api-key/create with valid session returns API key
    - Test: Protected endpoint with valid API key header returns 200
    - Test: Protected endpoint without auth returns 401
  </behavior>
  <action>
    Install Better Auth dependencies in packages/api:
    ```
    npm install better-auth @better-auth/api-key --workspace=packages/api
    ```

    IMPORTANT: Before writing any Better Auth code, read the official Better Auth docs to confirm the exact API for:
    - betterAuth() config with drizzleAdapter
    - emailAndPassword plugin config
    - GitHub social provider config
    - apiKey() plugin config
    - Hono handler mounting pattern
    - Session retrieval API (auth.api.getSession)

    **packages/api/src/auth.ts**: Create Better Auth instance:
    - `drizzleAdapter(db, { provider: "pg" })` from @repo/db
    - `emailAndPassword: { enabled: true }`
    - `socialProviders: { github: { clientId, clientSecret } }` from env vars
    - `plugins: [apiKey()]` from @better-auth/api-key
    - `trustedOrigins: [process.env.FRONTEND_URL]`
    - Export the `auth` instance

    **packages/api/src/routes/auth.ts**: Create Hono router:
    - Mount `auth.handler` on `["POST", "GET"], "/api/auth/**"`
    - The handler receives `c.req.raw` and returns the Response

    **packages/api/src/middleware/auth.ts**: Create `requireAuth` middleware:
    - Check for session cookie first (via `auth.api.getSession({ headers: c.req.raw.headers })`)
    - If no session, check for API key in `Authorization: Bearer <key>` header (via Better Auth API key verification)
    - If neither, return 401 `{ success: false, error: "Unauthorized" }`
    - If authenticated, set `c.set("user", user)` and `c.set("session", session)` on context
    - Create Hono variable types for TypeScript: define `Variables` type with `user` and `session`

    **packages/api/src/index.ts**: Update the existing Hono app:
    - Add `authRoutes` via `app.route("/", authRoutes)`
    - Keep existing health check

    **packages/api/src/__tests__/setup.ts**: Test setup:
    - Use the real test database (DATABASE_URL from .env.test or fall back to .env with a test database name)
    - Setup: run migrations, clear tables before each test
    - Teardown: close DB connection
    - Export helper to create authenticated test client (sign up + sign in, return cookies)

    **Test files**: Write integration tests using Vitest + Hono's `app.request()` test helper (no HTTP server needed). Each test uses the Hono app directly. Test sign-up, sign-in, session persistence, API key creation, and middleware protection.

    Note: GitHub OAuth test (AUTH-04) cannot be fully automated (requires browser redirect). Create a test file `auth-github.test.ts` with a test that verifies the GitHub OAuth redirect URL is generated correctly (GET /api/auth/sign-in/social returns redirect to github.com/login/oauth/authorize with correct client_id).
  </action>
  <verify>
    <automated>cd C:/Users/basti/Projekte/AI_Agent_GAME && npx vitest run packages/api/src/__tests__/auth-email.test.ts packages/api/src/__tests__/auth-session.test.ts packages/api/src/__tests__/api-keys.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Email sign-up creates user in database
    - Email sign-in returns session cookie
    - Session cookie retrieves user data
    - API key creation and verification works
    - requireAuth middleware blocks unauthenticated requests
    - GitHub OAuth redirect URL is generated with correct parameters
  </done>
</task>

<task type="auto">
  <name>Task 2: Next.js auth pages and API key management UI</name>
  <files>
    apps/web/package.json,
    apps/web/src/lib/auth-client.ts,
    apps/web/src/app/(auth)/layout.tsx,
    apps/web/src/app/(auth)/sign-in/page.tsx,
    apps/web/src/app/(auth)/sign-up/page.tsx,
    apps/web/src/app/dashboard/page.tsx,
    apps/web/src/app/settings/page.tsx,
    apps/web/src/components/auth-form.tsx,
    apps/web/src/components/api-key-manager.tsx
  </files>
  <action>
    Install Better Auth client in apps/web:
    ```
    npm install better-auth @better-auth/api-key --workspace=apps/web
    ```

    IMPORTANT: Read Better Auth React client docs before writing code. Confirm the exact API for `createAuthClient`, `useSession`, `signIn.email`, `signUp.email`, `signIn.social`, and `apiKey` client plugin.

    **apps/web/src/lib/auth-client.ts**: Create Better Auth client:
    - `createAuthClient({ baseURL: process.env.NEXT_PUBLIC_API_URL, plugins: [apiKeyClient()] })`
    - Export `authClient` and destructured hooks: `useSession`, `signIn`, `signUp`, `signOut`

    **apps/web/src/components/auth-form.tsx**: Reusable auth form component:
    - Props: `mode: "sign-in" | "sign-up"`
    - Email + password fields with client-side validation (email format, password min 8 chars)
    - Submit handler calls `signUp.email` or `signIn.email` from auth client
    - GitHub OAuth button that calls `signIn.social({ provider: "github" })`
    - Loading states, error display
    - Redirect to /dashboard on success using Next.js router

    **apps/web/src/app/(auth)/layout.tsx**: Centered layout for auth pages (simple flexbox center).

    **apps/web/src/app/(auth)/sign-up/page.tsx**: Renders AuthForm with mode="sign-up".

    **apps/web/src/app/(auth)/sign-in/page.tsx**: Renders AuthForm with mode="sign-in". Link to sign-up page.

    **apps/web/src/app/dashboard/page.tsx**: Protected page:
    - Uses `useSession()` hook -- redirects to /sign-in if no session
    - Shows "Welcome, {user.name}" and basic dashboard shell
    - Links to /settings and /agents (agents page built in Plan 03)

    **apps/web/src/app/settings/page.tsx**: API key management page:
    - Protected (redirects if no session)
    - Uses ApiKeyManager component

    **apps/web/src/components/api-key-manager.tsx**: API key management component:
    - "Create API Key" button with name input
    - Calls `authClient.apiKey.create({ name })` -- shows the full key ONCE after creation (warn user to copy it)
    - Lists existing API keys (name, prefix, created date, enabled status)
    - Revoke button per key (calls `authClient.apiKey.delete({ keyId })`)
    - Use Tailwind CSS for styling (install and configure Tailwind in apps/web if not already present)

    After creating all files:
    - Verify `npx turbo build` passes
    - Start dev servers (`npx turbo dev`) and manually confirm pages render
  </action>
  <verify>
    <automated>cd C:/Users/basti/Projekte/AI_Agent_GAME && npx turbo build --filter=@repo/web 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Next.js build succeeds
    - Sign-up page renders at /sign-up with email/password form and GitHub button
    - Sign-in page renders at /sign-in with email/password form and GitHub button
    - Dashboard page at /dashboard shows user info (when authenticated)
    - Settings page at /settings shows API key manager
    - Auth client correctly points to API server URL
  </done>
</task>

</tasks>

<verification>
- All auth integration tests pass: sign-up, sign-in, session, API keys
- Next.js build succeeds with all auth pages
- Better Auth server instance correctly configured with email/password + GitHub + API key plugins
- Auth middleware protects endpoints (returns 401 without credentials)
- Frontend auth client connects to API server
</verification>

<success_criteria>
- AUTH-01: User can sign up with email/password (tested via integration test + UI page)
- AUTH-02: Session persists via database session cookie (tested via get-session after sign-in)
- AUTH-03: API keys can be created, listed, and revoked (tested via integration test + UI)
- AUTH-04: GitHub OAuth redirect generates correct URL (tested), full flow requires GitHub OAuth App setup
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-agent-pipeline/01-02-SUMMARY.md`
</output>
