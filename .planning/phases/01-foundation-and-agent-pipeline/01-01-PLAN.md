---
phase: 01-foundation-and-agent-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - turbo.json
  - tsconfig.json
  - docker-compose.yml
  - .env.example
  - .gitignore
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - packages/api/package.json
  - packages/api/tsconfig.json
  - packages/api/src/index.ts
  - packages/engine/package.json
  - packages/engine/tsconfig.json
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/src/index.ts
  - packages/db/src/schema/users.ts
  - packages/db/src/schema/sessions.ts
  - packages/db/src/schema/accounts.ts
  - packages/db/src/schema/agents.ts
  - packages/db/src/schema/api-keys.ts
  - packages/db/drizzle.config.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/types/api.ts
  - packages/shared/src/types/agent.ts
  - packages/shared/src/index.ts
  - vitest.config.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04

must_haves:
  truths:
    - "Turborepo monorepo builds successfully with all packages resolving"
    - "PostgreSQL runs via Docker Compose and accepts connections"
    - "Database schema migrations run and create all tables (users, sessions, accounts, agents, api_keys)"
    - "Shared types are importable from any package via @repo/shared"
    - "Hono API server starts and responds to health check"
    - "Next.js app starts and renders a page"
  artifacts:
    - path: "package.json"
      provides: "Root workspace configuration with Turborepo"
      contains: "workspaces"
    - path: "turbo.json"
      provides: "Turborepo pipeline config"
      contains: "pipeline"
    - path: "docker-compose.yml"
      provides: "PostgreSQL local dev container"
      contains: "postgres"
    - path: "packages/db/src/schema/agents.ts"
      provides: "Agent table schema"
      contains: "pgTable"
    - path: "packages/shared/src/types/api.ts"
      provides: "API response envelope type"
      exports: ["ApiResponse"]
    - path: "packages/shared/src/types/agent.ts"
      provides: "Agent-related types"
      exports: ["AgentSourceType"]
    - path: "packages/api/src/index.ts"
      provides: "Hono app entry point"
      exports: ["default"]
  key_links:
    - from: "packages/api/src/index.ts"
      to: "@repo/db"
      via: "package dependency"
      pattern: "@repo/db"
    - from: "packages/api/src/index.ts"
      to: "@repo/shared"
      via: "package dependency"
      pattern: "@repo/shared"
---

<objective>
Scaffold the Turborepo monorepo with all packages (web, api, db, engine, shared), set up PostgreSQL via Docker Compose, define the complete database schema with Drizzle ORM, create shared TypeScript types, and configure Vitest for testing.

Purpose: Every subsequent plan depends on this foundation -- package structure, database tables, shared types, and dev infrastructure.
Output: Working monorepo where `turbo build` succeeds, PostgreSQL is running, schema migrations have been applied, and both the API server and Next.js app start.
</objective>

<execution_context>
@C:/Users/basti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/basti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-agent-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Turborepo monorepo with all packages and Docker Compose</name>
  <files>
    package.json, turbo.json, tsconfig.json, .env.example, .gitignore, docker-compose.yml,
    apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json,
    apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx,
    packages/api/package.json, packages/api/tsconfig.json, packages/api/src/index.ts,
    packages/engine/package.json, packages/engine/tsconfig.json,
    packages/db/package.json, packages/db/tsconfig.json,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts,
    vitest.config.ts
  </files>
  <action>
    Create the Turborepo monorepo from scratch (do NOT use `create-turbo` -- scaffold manually for full control):

    1. **Root package.json**: npm workspaces pointing to `apps/*` and `packages/*`. Scripts: `dev`, `build`, `test`, `lint`. DevDeps: `turbo`, `typescript`, `vitest`, `@vitest/coverage-v8`, `tsx`, `tsup`, `prettier`, `@types/node`.

    2. **turbo.json**: Pipeline with `build` (dependsOn: `^build`), `dev` (persistent: true), `test`, `lint`. Use Turborepo v2 config format.

    3. **Root tsconfig.json**: Base TypeScript config extended by all packages. Target: ES2022, module: NodeNext, strict: true.

    4. **.gitignore**: node_modules, dist, .next, .turbo, .env, coverage, drizzle/meta.

    5. **.env.example**: DATABASE_URL (postgres://postgres:postgres@localhost:5432/ai_agent_arena), GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, FRONTEND_URL (http://localhost:3000), API_URL (http://localhost:3001), BETTER_AUTH_SECRET.

    6. **docker-compose.yml**: PostgreSQL 16 container with user postgres, password postgres, database ai_agent_arena, port 5432. Volume for data persistence.

    7. **apps/web**: Minimal Next.js 15 app with App Router. package.json with next, react, react-dom. A simple layout.tsx and page.tsx that renders "AI Agent Arena" heading. Name: `@repo/web`.

    8. **packages/api**: Hono 4.x API server. package.json with hono, @hono/node-server, zod, nanoid, pino, pino-pretty (dev). Entry point src/index.ts creates Hono app with CORS (origin from FRONTEND_URL env), logger middleware, and a GET /health route returning `{ status: "ok" }`. Start script runs via tsx in dev, tsup build for prod. Name: `@repo/api`. Depends on `@repo/db` and `@repo/shared`.

    9. **packages/engine**: Empty package for sandbox POC (Plan 03). package.json with name `@repo/engine`, no dependencies yet. tsconfig.json extending root.

    10. **packages/db**: Drizzle ORM package. package.json with drizzle-orm, postgres (porsager), drizzle-kit (dev). Name: `@repo/db`. tsconfig.json extending root.

    11. **packages/shared**: Shared types package. package.json with name `@repo/shared`, no runtime deps. tsconfig.json extending root. Create src/index.ts that re-exports from types/*.

    12. **vitest.config.ts**: Root Vitest workspace config. Configure test include patterns for all packages.

    After creating all files, run:
    - `npm install`
    - `docker compose up -d` (start PostgreSQL)
    - `npx turbo build` (verify monorepo builds)

    IMPORTANT: Do NOT install `better-auth`, `isolated-vm`, or `esbuild` yet -- those belong to Plans 02 and 03. Only install the base infrastructure dependencies.
  </action>
  <verify>
    <automated>cd C:/Users/basti/Projekte/AI_Agent_GAME && npx turbo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npx turbo build` completes with 0 errors
    - `docker compose ps` shows PostgreSQL running
    - All 5 packages exist with valid package.json and tsconfig.json
    - .env.example contains all required variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Database schema, shared types, and migration</name>
  <files>
    packages/db/src/schema/users.ts, packages/db/src/schema/sessions.ts,
    packages/db/src/schema/accounts.ts, packages/db/src/schema/agents.ts,
    packages/db/src/schema/api-keys.ts, packages/db/src/schema/index.ts,
    packages/db/src/index.ts, packages/db/drizzle.config.ts,
    packages/shared/src/types/api.ts, packages/shared/src/types/agent.ts,
    packages/shared/src/index.ts
  </files>
  <action>
    1. **Better Auth compatible schema**: Run `npx @better-auth/cli generate` to see the expected table structure, then create Drizzle schema files that match. Better Auth expects specific table names and columns for users, sessions, accounts, and verification tokens. Create these schema files manually matching Better Auth's expected structure:

    **packages/db/src/schema/users.ts**: `user` table with columns: id (text PK, nanoid default), name (text), email (text, unique, not null), emailVerified (boolean), image (text), createdAt (timestamp), updatedAt (timestamp).

    **packages/db/src/schema/sessions.ts**: `session` table with: id (text PK), expiresAt (timestamp not null), token (text unique not null), ipAddress (text), userAgent (text), userId (text not null, references user.id on delete cascade), createdAt (timestamp), updatedAt (timestamp).

    **packages/db/src/schema/accounts.ts**: `account` table with: id (text PK), accountId (text not null), providerId (text not null), userId (text not null, references user.id on delete cascade), accessToken (text), refreshToken (text), idToken (text), accessTokenExpiresAt (timestamp), refreshTokenExpiresAt (timestamp), scope (text), password (text), createdAt (timestamp), updatedAt (timestamp).

    **packages/db/src/schema/api-keys.ts**: `apikey` table with: id (text PK), name (text), start (text), prefix (text), key (text not null), userId (text not null, references user.id on delete cascade), refillInterval (text), refillAmount (integer), lastRefillAt (timestamp), enabled (boolean default true), rateLimitEnabled (boolean default false), rateLimitTimeWindow (integer), rateLimitMax (integer), requestCount (integer default 0), remaining (integer), lastRequest (timestamp), expiresAt (timestamp), createdAt (timestamp), updatedAt (timestamp), permissions (text), metadata (text).

    **packages/db/src/schema/agents.ts**: Custom application table. `agents` table with: id (text PK, nanoid default), userId (text not null, references user.id on delete cascade), name (text not null), description (text), sourceType (text not null, "upload" or "github"), sourceUrl (text, nullable, GitHub URL if linked), originalCode (text not null, TypeScript source), compiledCode (text not null, compiled JS), version (integer default 1), isActive (boolean default true), createdAt (timestamp defaultNow), updatedAt (timestamp defaultNow).

    **packages/db/src/schema/index.ts**: Re-export all schemas.

    **packages/db/src/index.ts**: Create and export the Drizzle database client using `postgres` driver. Read DATABASE_URL from env. Export `db` instance and all schemas.

    **packages/db/drizzle.config.ts**: Drizzle Kit config pointing to schema directory, PostgreSQL dialect, DATABASE_URL from env.

    2. **Shared types**:

    **packages/shared/src/types/api.ts**:
    ```typescript
    export interface ApiResponse<T> {
      success: boolean
      data?: T
      error?: string
    }
    ```

    **packages/shared/src/types/agent.ts**:
    ```typescript
    export type AgentSourceType = "upload" | "github"

    export interface AgentMetadata {
      id: string
      name: string
      description: string | null
      sourceType: AgentSourceType
      sourceUrl: string | null
      version: number
      isActive: boolean
      createdAt: Date
      updatedAt: Date
    }

    export interface AgentUploadInput {
      name: string
      description?: string
      code: string
    }

    export interface AgentGitHubLinkInput {
      name: string
      description?: string
      repoUrl: string
      filePath: string
    }
    ```

    **packages/shared/src/index.ts**: Re-export all types.

    3. **Run migration**: Copy .env.example to .env (if not exists), then run `npx drizzle-kit push` to apply schema to the running PostgreSQL.

    4. **Verify**: Run a quick script that connects to the database and queries information_schema to confirm all tables exist.
  </action>
  <verify>
    <automated>cd C:/Users/basti/Projekte/AI_Agent_GAME && npx tsx -e "import { db } from '@repo/db'; import { sql } from 'drizzle-orm'; const r = await db.execute(sql\`SELECT table_name FROM information_schema.tables WHERE table_schema='public'\`); console.log(r.map(t => t.table_name).sort().join(', ')); process.exit(0)" 2>&1</automated>
  </verify>
  <done>
    - All 5 tables exist in PostgreSQL: user, session, account, apikey, agents
    - Drizzle schema files compile without TypeScript errors
    - Shared types are importable via `@repo/shared`
    - `npx turbo build` still passes with the new schema and types
  </done>
</task>

</tasks>

<verification>
- `npx turbo build` completes successfully (all packages)
- `docker compose ps` shows PostgreSQL running
- Database contains all 5 tables (user, session, account, apikey, agents)
- Hono API responds to GET /health with `{ status: "ok" }`
- Next.js app renders at http://localhost:3000
- Shared types importable from `@repo/shared`
</verification>

<success_criteria>
- Monorepo structure matches research architecture with apps/web, packages/api, packages/db, packages/engine, packages/shared
- PostgreSQL database has all tables matching Better Auth expectations + custom agents table
- TypeScript compilation succeeds across all packages
- Shared types (ApiResponse, AgentMetadata, etc.) are importable
- Development servers (web + api) start without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-agent-pipeline/01-01-SUMMARY.md`
</output>
