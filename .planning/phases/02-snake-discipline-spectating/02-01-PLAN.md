---
phase: 02-snake-discipline-spectating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/specs/snake.schema.ts
  - packages/shared/src/specs/snake.json
  - packages/shared/src/specs/snake-starter.ts
  - packages/shared/src/specs/__tests__/snake-schema.test.ts
  - packages/shared/src/types/match.ts
  - packages/shared/src/types/snake.ts
  - packages/shared/src/index.ts
  - packages/engine/src/disciplines/snake/engine.ts
  - packages/engine/src/disciplines/snake/rules.ts
  - packages/engine/src/disciplines/snake/types.ts
  - packages/engine/src/disciplines/snake/__tests__/engine.test.ts
  - packages/engine/src/disciplines/snake/__tests__/determinism.test.ts
  - packages/engine/src/index.ts
  - packages/engine/package.json
autonomous: true
requirements: [GAME-01, GAME-03]

must_haves:
  truths:
    - "Snake engine produces valid game state with correct collision detection and scoring"
    - "Same seed and same agent moves produce identical frame sequences (deterministic)"
    - "Zod schemas generate valid JSON Schema that matches TypeScript types"
    - "Starter agent compiles and returns valid SnakeMove when given SnakeGameState"
  artifacts:
    - path: "packages/shared/src/specs/snake.schema.ts"
      provides: "Zod schemas for SnakeGameState and SnakeMove (single source of truth)"
      exports: ["snakeGameStateSchema", "snakeMoveSchema", "SnakeGameState", "SnakeMove", "snakeGameStateJsonSchema", "snakeMoveJsonSchema"]
    - path: "packages/shared/src/specs/snake.json"
      provides: "Generated JSON Schema file for external tools"
    - path: "packages/shared/src/specs/snake-starter.ts"
      provides: "Starter agent TypeScript source (greedy food-chasing)"
    - path: "packages/engine/src/disciplines/snake/engine.ts"
      provides: "Deterministic tick-based Snake engine using prando PRNG"
      min_lines: 80
    - path: "packages/engine/src/disciplines/snake/rules.ts"
      provides: "Snake game configuration defaults"
    - path: "packages/engine/src/disciplines/snake/__tests__/engine.test.ts"
      provides: "Core engine tests including collision, scoring, food spawning"
    - path: "packages/engine/src/disciplines/snake/__tests__/determinism.test.ts"
      provides: "Determinism verification test"
  key_links:
    - from: "packages/engine/src/disciplines/snake/engine.ts"
      to: "packages/shared/src/specs/snake.schema.ts"
      via: "imports SnakeGameState, SnakeMove types"
      pattern: "import.*SnakeGameState.*from.*@repo/shared"
    - from: "packages/engine/src/disciplines/snake/engine.ts"
      to: "prando"
      via: "seeded PRNG for food spawning"
      pattern: "new Prando"
---

<objective>
Create the Snake game specification (Zod schemas, TypeScript types, JSON Schema, starter agent) and the deterministic tick-based Snake game engine.

Purpose: This is the foundation for all match execution, spectating, and replay. The spec defines the contract between agents and the engine. The engine must be deterministic (same seed = same result) to enable faithful replays.

Output: Zod schemas as single source of truth, generated JSON Schema, starter agent template, deterministic Snake engine with tests proving correctness and determinism.
</objective>

<execution_context>
@C:/Users/basti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/basti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-snake-discipline-spectating/02-RESEARCH.md

<interfaces>
<!-- Existing exports from @repo/engine that the snake engine will extend -->

From packages/engine/src/index.ts:
```typescript
export { compileTypeScript } from "./compiler.js"
export {
  executeInSandbox,
  TimeoutError,
  MemoryLimitError,
  type SandboxOptions,
  type SandboxResult,
} from "./sandbox.js"
export const ENGINE_VERSION = "0.1.0"
```

From packages/engine/src/sandbox.ts:
```typescript
export interface SandboxOptions {
  memoryLimitMb: number
  timeoutMs: number
}
export interface SandboxResult {
  result: unknown
  cpuTimeNs: bigint
  wallTimeNs: bigint
}
export async function executeInSandbox(
  compiledJs: string,
  gameState: unknown,
  options: Partial<SandboxOptions>
): Promise<SandboxResult>
```

From packages/shared/src/types/agent.ts:
```typescript
export type AgentSourceType = "upload" | "github"
export interface AgentMetadata { id: string; name: string; /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snake specification -- Zod schemas, types, JSON Schema, and starter agent</name>
  <files>
    packages/shared/src/specs/snake.schema.ts,
    packages/shared/src/specs/snake.json,
    packages/shared/src/specs/snake-starter.ts,
    packages/shared/src/specs/__tests__/snake-schema.test.ts,
    packages/shared/src/types/match.ts,
    packages/shared/src/types/snake.ts,
    packages/shared/src/index.ts,
    packages/shared/package.json
  </files>
  <behavior>
    - Test 1: snakeGameStateSchema validates a correct game state object
    - Test 2: snakeGameStateSchema rejects invalid state (missing fields, wrong types)
    - Test 3: snakeMoveSchema validates {direction: "up"} and {direction: "left", reasoning: "toward food"}
    - Test 4: snakeMoveSchema rejects invalid direction (e.g., "diagonal")
    - Test 5: snakeGameStateJsonSchema is valid JSON Schema with correct $schema field
    - Test 6: snakeMoveJsonSchema is valid JSON Schema
    - Test 7: Generated snake.json file matches the exported JSON Schema objects
    - Test 8: SnakeGameState type inferred from schema matches expected shape (compile-time check via assignment)
    - Test 9: Starter agent source is valid TypeScript string containing "function move"
  </behavior>
  <action>
    1. Install zod-to-json-schema in packages/shared: `npm install zod-to-json-schema --workspace=packages/shared`

    2. Create `packages/shared/src/specs/snake.schema.ts`:
       - Define `snakeGameStateSchema` (Zod object): tick (int, nonneg), snake (array of {x: int, y: int}), food ({x: int, y: int}), direction (enum: up/down/left/right), score (int, nonneg), alive (boolean), gridWidth (int, positive), gridHeight (int, positive)
       - Define `snakeMoveSchema` (Zod object): direction (enum: up/down/left/right), reasoning (string, max 200, optional)
       - Export inferred TypeScript types: `SnakeGameState`, `SnakeMove`
       - Generate and export JSON Schema via `zodToJsonSchema()`
       - Export a `snakeSpec` object combining both schemas and metadata (discipline name, version)

    3. Create `packages/shared/src/types/snake.ts` re-exporting the Zod-inferred types for convenience.

    4. Create `packages/shared/src/types/match.ts`:
       - `MatchStatus` type: "queued" | "running" | "completed" | "error"
       - `TerminationReason` type: "completed" | "timeout" | "crash" | "death"
       - `MatchMetadata` interface: id, userId, agentId, discipline, seed, status, score, ticksPlayed, terminationReason, createdAt, completedAt

    5. Create `packages/shared/src/specs/snake-starter.ts`:
       - Export `SNAKE_STARTER_AGENT_SOURCE` as a string constant containing the starter agent TypeScript code (greedy food-chasing using Manhattan distance, includes reasoning field)
       - The code must define a `move(state: SnakeGameState): SnakeMove` function

    6. Create a script or build step that writes `packages/shared/src/specs/snake.json` containing the JSON Schema objects (run during build or as a simple node script). For simplicity, create the JSON file directly in Task action.

    7. Update `packages/shared/src/index.ts` to re-export from specs and new type files.

    8. Write tests FIRST in `packages/shared/src/specs/__tests__/snake-schema.test.ts` covering all behaviors above.
  </action>
  <verify>
    <automated>npx vitest run packages/shared/src/specs/__tests__/snake-schema.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Zod schemas validate correct input and reject invalid input
    - JSON Schema generated from Zod matches expected structure
    - snake.json file exists with valid JSON Schema
    - Starter agent source string contains move function
    - All types exported from @repo/shared
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic Snake game engine with tick-based loop</name>
  <files>
    packages/engine/src/disciplines/snake/engine.ts,
    packages/engine/src/disciplines/snake/rules.ts,
    packages/engine/src/disciplines/snake/types.ts,
    packages/engine/src/disciplines/snake/__tests__/engine.test.ts,
    packages/engine/src/disciplines/snake/__tests__/determinism.test.ts,
    packages/engine/src/index.ts,
    packages/engine/package.json
  </files>
  <behavior>
    - Test 1: Engine initializes with snake at correct position and length
    - Test 2: Snake moves in the correct direction when applyMove is called
    - Test 3: Snake grows when it eats food (score increments, length increases)
    - Test 4: Snake dies on wall collision (alive = false)
    - Test 5: Snake dies on self collision
    - Test 6: Food respawns at a new position after being eaten (not on snake body)
    - Test 7: Opposite direction move is ignored (can't reverse into self)
    - Test 8: Engine reaches maxTicks and terminates with reason "completed"
    - Test 9: getFrame() returns complete frame data including agentMove and agentReasoning
    - Test 10 (determinism): Two engines with same seed and same moves produce identical frame sequences
    - Test 11 (determinism): Two engines with different seeds produce different food positions
  </behavior>
  <action>
    1. Install prando in packages/engine: `npm install prando --workspace=packages/engine`

    2. Create `packages/engine/src/disciplines/snake/types.ts`:
       - `SnakeGameConfig` interface: gridWidth (default 20), gridHeight (default 20), maxTicks (default 500), initialLength (default 3), seed (string), tickRate (default 5)
       - `SnakeFrame` interface: tick, state (SnakeGameState from @repo/shared), agentMove (string | null), agentReasoning (string | null)
       - Import SnakeGameState and SnakeMove from @repo/shared

    3. Create `packages/engine/src/disciplines/snake/rules.ts`:
       - Export `DEFAULT_SNAKE_CONFIG` with sensible defaults
       - Export `OPPOSITE_DIRECTIONS` map for direction reversal check
       - ALL arithmetic must use integers only -- no floating point

    4. Create `packages/engine/src/disciplines/snake/engine.ts`:
       - `createSnakeEngine(config: SnakeGameConfig)` factory function returning engine object
       - Use `new Prando(config.seed)` for ALL randomness (food placement)
       - Integer-only math for all positions and scores
       - Methods: `getState()`, `applyMove(move: SnakeMove)` returning SnakeFrame, `isAlive()`, `getTick()`, `getScore()`, `getTerminationReason()`
       - Collision detection: wall boundaries (0 to gridWidth-1, 0 to gridHeight-1), self-collision (head hits any body segment)
       - Food spawning: use PRNG to pick random empty cell (not on snake body)
       - Direction validation: ignore move if it's the opposite of current direction
       - Snake movement: add new head in direction, remove tail (unless food eaten)
       - Track agentMove and agentReasoning per frame

    5. Update `packages/engine/src/index.ts` to export `createSnakeEngine` and snake types.

    6. Write tests FIRST:
       - `engine.test.ts`: Tests 1-9 covering initialization, movement, collision, scoring, food, direction validation, termination, frame data
       - `determinism.test.ts`: Tests 10-11 covering same-seed identical results and different-seed different results
  </action>
  <verify>
    <automated>npx vitest run packages/engine/src/disciplines/snake/__tests__/ --reporter=verbose</automated>
  </verify>
  <done>
    - Snake engine produces valid game states with correct collision and scoring
    - Same seed + same moves = identical frame sequences (determinism verified)
    - Different seeds = different food positions
    - Engine terminates at maxTicks with reason "completed"
    - All integer arithmetic (no floating point in engine)
    - Engine exported from @repo/engine
  </done>
</task>

</tasks>

<verification>
- `npx vitest run packages/shared/src/specs/__tests__/ --reporter=verbose` -- all schema tests pass
- `npx vitest run packages/engine/src/disciplines/snake/__tests__/ --reporter=verbose` -- all engine + determinism tests pass
- `npx turbo build` -- monorepo builds successfully with new packages
- `cat packages/shared/src/specs/snake.json` -- valid JSON Schema file exists
</verification>

<success_criteria>
- Snake game specification fully defined via Zod schemas with JSON Schema generation
- Deterministic Snake engine passes all unit tests including determinism verification
- Starter agent template exists and contains valid move function
- All new types exported from @repo/shared and @repo/engine
- Zero floating-point arithmetic in engine code
</success_criteria>

<output>
After completion, create `.planning/phases/02-snake-discipline-spectating/02-01-SUMMARY.md`
</output>
